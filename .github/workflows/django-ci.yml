name: CI/CD - DESATIVADO

# WORKFLOW DESATIVADO - O Render faz o deploy automaticamente
# Para re-ativar, remova a linha abaixo

on:
  # Workflow desativado
  workflow_dispatch:

jobs:
  # Todos os jobs estão desativados
  # O Render faz deployment e migrações automaticamente
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow desativado
        run: echo "CI/CD desativado. Render gerencia deploys automaticamente."
